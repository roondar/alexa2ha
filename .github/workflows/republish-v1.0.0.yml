name: Temporary release cleanup

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-24.04
    steps:
      - name: No-op
        run: echo "Temporary workflow disabled"
